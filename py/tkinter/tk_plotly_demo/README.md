!!!!!!This project is not working!!!!!!

This project attempts to embed Plotly into a tkinter GUI.

The approach:
- Plotly naturally renders on HTML.
- Use tkhtmlview libraries to render Plotly HTML files in tkinter.

Unfortunately, this is only displaying raw HTML text